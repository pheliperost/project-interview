name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: client/package-lock.json

      - name: Restore and test .NET
        run: |
          dotnet restore BlaInterview.slnx
          dotnet build BlaInterview.slnx --configuration Release --no-restore
          dotnet test BlaInterview.slnx --configuration Release --no-build

      - name: Install and verify client
        working-directory: client
        run: |
          npm install --no-audit --no-fund
          npm run verify
          npm run build
